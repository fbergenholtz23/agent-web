---
applications:
  - name: ericsson-sox-agent-web
    memory: 256M
    instances: 1
    buildpacks:
      - python_buildpack
    command: uvicorn main:app --host 0.0.0.0 --port $PORT
    path: backend
    env:
      LIVE_MODE:        true
      SOX_PIPELINE_URL: ((SOX_PIPELINE_URL))
