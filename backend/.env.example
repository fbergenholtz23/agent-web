# Copy this to .env and fill in your values

# Set to true to proxy requests to the SOX pipeline CF app
LIVE_MODE=false

# URL of the deployed sox-pipeline CF app (required when LIVE_MODE=true)
SOX_PIPELINE_URL=https://ericsson-sox-pipeline.cfapps.<your-cf-domain>.com
